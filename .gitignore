# env / secrets
.env
*.env.local

# python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# sqlite runtime artifacts (canonical seed = backend/app/data/seed.db IS committed;
# runtime db, journals, and stray empty seed.db in the wrong dirs are not)
backend/app/data/pitchside.db
backend/seed.db
backend/app/seed.db
*.db-journal
*.db-wal
*.db-shm

# downloaded raw data (rebuildable via scripts)
backend/scripts/cache/

# node
node_modules/
frontend/dist/

# os
.DS_Store
